# OrderDesk — returns service

The slice of OrderDesk that handles returns and refunds.

`main` is protected: push a branch and open a pull request. Direct pushes are refused.

## Layout

```
src/returns.js        opening and approving returns
docs/returns-policy.md the policy the code is meant to implement
```
